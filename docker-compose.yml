services:
  golang:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    networks:
      - app-network

  python:
    build:
      context: .
      dockerfile: Dockerfile-python
    ports:
      - "53:53"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge